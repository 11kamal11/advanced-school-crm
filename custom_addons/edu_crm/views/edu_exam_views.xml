<odoo>
    <!-- ── Search view ──────────────────────────────────────────── -->
    <record id="view_edu_exam_search" model="ir.ui.view">
        <field name="name">edu.exam.search</field>
        <field name="model">edu.exam</field>
        <field name="arch" type="xml">
            <search string="Exams">
                <field name="name"/>
                <field name="class_id"/>
                <field name="subject_id"/>
                <field name="academic_year_id"/>
                <filter string="Scheduled"  name="draft"     domain="[('state','=','draft')]"/>
                <filter string="Ongoing"    name="ongoing"   domain="[('state','=','ongoing')]"/>
                <filter string="Completed"  name="completed" domain="[('state','=','completed')]"/>
                <filter string="Cancelled"  name="cancelled" domain="[('state','=','cancelled')]"/>
                <separator/>
                <filter string="Unit Test"  name="unit_test" domain="[('exam_type','=','unit_test')]"/>
                <filter string="Mid Term"   name="mid_term"  domain="[('exam_type','=','mid_term')]"/>
                <filter string="Final"      name="final"     domain="[('exam_type','=','final')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_class"   string="Class"      context="{'group_by':'class_id'}"/>
                    <filter name="group_subject" string="Subject"    context="{'group_by':'subject_id'}"/>
                    <filter name="group_state"   string="Status"     context="{'group_by':'state'}"/>
                    <filter name="group_type"    string="Exam Type"  context="{'group_by':'exam_type'}"/>
                    <filter name="group_year"    string="Acad. Year" context="{'group_by':'academic_year_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ── List view ────────────────────────────────────────────── -->
    <record id="view_edu_exam_list" model="ir.ui.view">
        <field name="name">edu.exam.list</field>
        <field name="model">edu.exam</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="exam_type"/>
                <field name="class_id"/>
                <field name="subject_id"/>
                <field name="exam_date"/>
                <field name="average_marks"   optional="show"/>
                <field name="pass_percentage" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'completed'"
                       decoration-info="state == 'ongoing'"
                       decoration-muted="state in ('draft','cancelled')"/>
            </list>
        </field>
    </record>

    <!-- ── Kanban view ──────────────────────────────────────────── -->
    <record id="view_edu_exam_kanban" model="ir.ui.view">
        <field name="name">edu.exam.kanban</field>
        <field name="model">edu.exam</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="name"/>
                <field name="class_id"/>
                <field name="subject_id"/>
                <field name="exam_date"/>
                <field name="exam_type"/>
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <div class="fw-bold"><field name="name"/></div>
                        <div class="text-muted small mt-1">
                            <i class="fa fa-users me-1"/>
                            <field name="class_id"/>
                        </div>
                        <div class="text-muted small">
                            <i class="fa fa-book me-1"/>
                            <field name="subject_id"/>
                        </div>
                        <div class="text-muted small mt-1">
                            <i class="fa fa-calendar me-1"/>
                            <field name="exam_date"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ── Form view ────────────────────────────────────────────── -->
    <record id="view_edu_exam_form" model="ir.ui.view">
        <field name="name">edu.exam.form</field>
        <field name="model">edu.exam</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_start"    string="Start"    type="object"
                            invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_complete" string="Complete" type="object"
                            invisible="state != 'ongoing'" class="oe_highlight"/>
                    <button name="action_cancel"   string="Cancel"   type="object"
                            invisible="state in ('completed','cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,ongoing,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="exam_type"/>
                            <field name="class_id"/>
                            <field name="subject_id"/>
                            <field name="academic_year_id"/>
                            <field name="exam_date"/>
                        </group>
                        <group>
                            <field name="max_marks"/>
                            <field name="passing_marks"/>
                            <field name="average_marks"/>
                            <field name="highest_marks"/>
                            <field name="pass_percentage"/>
                        </group>
                    </group>
                    <field name="description"/>
                    <field name="result_ids">
                        <list editable="bottom">
                            <field name="student_id"/>
                            <field name="marks_obtained"/>
                            <field name="percentage" readonly="1"/>
                            <field name="grade"       readonly="1"/>
                            <field name="is_passed"   readonly="1"/>
                            <field name="remarks"/>
                        </list>
                    </field>
                </sheet>
                <chatter reload_on_post="True"/>
            </form>
        </field>
    </record>

    <!-- ── Action ───────────────────────────────────────────────── -->
    <record id="action_edu_exam" model="ir.actions.act_window">
        <field name="name">Exams</field>
        <field name="res_model">edu.exam</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_edu_exam_search"/>
    </record>
</odoo>
