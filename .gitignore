# Local/custom development files
custom_addons/whatsapp_lead_automation/
page_xml_export.md

# Runtime data: filestore, sessions, downloaded addon caches -- never commit user data
# /data/ anchored to repo root so module data/ directories are NOT ignored
/data/

# Logs
/logs/

# Contains real credentials (db password, master password)
conf/odoo-school-crm.conf

# Python
__pycache__/
*.pyc
*.pyo

# Editor / OS cruft
.DS_Store
*.swp

# Contains a live Odoo API key -- never commit. Copy .mcp.json.example instead.
.mcp.json

# Local virtualenv for the MCP server
mcp_server/venv/
